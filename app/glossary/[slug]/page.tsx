import type {Metadata} from "next";import{notFound}from"next/navigation";import SiteHeader from"../../components/SiteHeader";import{getGlossaryTerm,glossaryTerms}from"../../data/glossary";
export function generateStaticParams(){return glossaryTerms.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const t=getGlossaryTerm(slug);if(!t)return{};const description=`${t.name}の意味を投資初心者向けに分かりやすく説明します。`;return{title:`${t.name}とは？`,description,alternates:{canonical:`/glossary/${slug}`},openGraph:{title:`${t.name}とは？`,description},twitter:{title:`${t.name}とは？`,description}}}
export default async function TermPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const t=getGlossaryTerm(slug);if(!t)notFound();return <main className="article-page"><SiteHeader/><article><nav><a href="/">トップ</a><span>›</span><a href="/glossary">用語集</a><span>›</span>{t.name}</nav><header><span>投資用語</span><h1>{t.name}とは？</h1><p>{t.meaning}</p></header><section className="quick-answer"><b>ひとことで</b><p>{t.meaning}</p></section><section className="article-section"><span>01</span><div><h2>具体例</h2><p>{t.example}</p></div></section><section className="article-section"><span>02</span><div><h2>注意点</h2><p>{t.caution}</p></div></section><aside className="article-next"><p>関連教材</p><h2>仕組みをまとまった流れで学ぶ</h2><a href={t.lesson}>関連する教材へ →</a></aside></article></main>}
